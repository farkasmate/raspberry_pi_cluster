apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: home-assistant

spec:
  storageClassName: wd-iscsi
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi
